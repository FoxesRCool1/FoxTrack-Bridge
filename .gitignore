# Built binaries
foxtrack-bridge
foxtrack-bridge.exe
FoxTrack-Bridge-*

# Local build artifacts (release binaries are built by CI and attached to Releases)
dist/

# Tailwind standalone CLI (dev-time asset generation only)
web/tailwindcss

# Config with secrets — users should never commit this
config/config.json

# Go
*.test
*.out
vendor/

# OS
.DS_Store
Thumbs.db
foxtrack-bridge-linux

# Logs
build.log
*.prev

# Local development tool files (kept on disk, not published)
CLAUDE.md
AGENTS.md
.claude/
.llm/
.roo/
.rooignore
